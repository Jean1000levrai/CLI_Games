#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>

#include "logic.h"
#include "interface.h"

int main() {
    

    // for (int i = 0; i<10; i++) {
    //     fflush(stdout);
    //     sleep(1);
    //     printHangman(i);
    // }
    int n = 323565;
    printf("%d\n", n);
    // readWord();
    game();
    return 0;
}